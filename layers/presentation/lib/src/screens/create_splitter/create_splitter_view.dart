import 'package:flutter/material.dart';

class CreateSplitterView extends StatelessWidget {
  const CreateSplitterView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'this is the Create Splitter view.',
      ),
    );
  }
}
